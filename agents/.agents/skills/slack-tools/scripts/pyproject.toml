[project]
name = "slack-tools"
version = "0.1.0"
description = "Slack CLI tool for sending/reading messages, files, and searching"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=44.0.0",
    "secretstorage>=3.5.0",
]
